[project]
name = "alertflow"
version = "0.3.0"
description = "SOC alert triage workflow with enrichment and live integration"
requires-python = ">=3.11"
dependencies = [
    "rich>=15.0.0",
    "httpx>=0.27.0",
    "typer>=0.24.1",
]

[project.optional-dependencies]
live = [
    "httpx>=0.27.0",
]
